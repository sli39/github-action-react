name: Test React App
on: push
jobs:
  runs-ob: ubuntu-latest
  steps:
        - name: Get reposit code
        - uses: actions/checkout@v6
        - name: Install dependecies
          run: npm ci
        - name: Test application
          run: npm run test

